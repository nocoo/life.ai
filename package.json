{
  "name": "life.ai",
  "private": true,
  "type": "module",
  "scripts": {
    "ut": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov && bun run scripts/db/coverage-check.ts",
    "lint": "eslint . --ext .ts",
    "prepare": "husky",
    "db:init": "bun run scripts/db/init.ts",
    "db:load": "bun run scripts/db/cli.ts load",
    "db:agg": "bun run scripts/db/cli.ts agg",
    "db:refresh": "bun run scripts/db/refresh.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "eslint": "^9.20.1",
    "eslint-config-prettier": "^10.0.1",
    "@eslint/js": "^9.20.0",
    "typescript": "^5.7.3",
    "husky": "^9.1.7"
  }
}
